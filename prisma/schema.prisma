generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mongodb"
    url      = "mongodb+srv://thegarageDB:thegarageDB@thegaragedb.dpbeylo.mongodb.net/myDatabase?retryWrites=true&w=majority"
    //env("DATABASE_URL")
}

model User {
    user_id String @id @default(auto()) @map("_id") @db.ObjectId
    name    String

    @@map("users")
}
